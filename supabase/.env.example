# Supabase Edge Functions Secrets
# Set these locally with: supabase secrets set <KEY>=<VALUE>
# Or in production via Supabase dashboard > Edge Functions > Secrets

# TMDB API Token (required for seeding movies)
# Get this from https://www.themoviedb.org/settings/api
TMDB_API_TOKEN=your-tmdb-api-token-here
